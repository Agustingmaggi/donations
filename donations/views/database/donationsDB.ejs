<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <link rel="stylesheet" href="donacionesDB.css">
    <title>Document</title>
</head>
<body>
  <a href="/database/agregarData">Agregar Usuario a la base de datos</a> <br>
  <a href="/database/agregarProyecto">Agregar Proyecto a la base de datos</a> <br><br><br>

    Aca mostramos las donaciones de cada usuario
    <ul>
      <% for (let i = 0; i < data.length; i++) { %>
        <li>
          <% if (data[i].name) { %>
            Nombre: <%= data[i].name %>
          <% } %>
          <% if (data[i].email) { %>
            Correo electrónico: <%= data[i].email %>
          <% } %>
          <ul>
            <% for (let j = 0; j < data[i].projects.length; j++) { %>
              <li>
                <% if (data[i].projects[j]) { %>
                  Proyecto: <%= data[i].projects[j].name %>
                <% } %>
              </li>
            <% } %>
          </ul>
        </li>
      <% } %>
    </ul>
    Aca mostramos los donantes de cada proyecto <br><br>

    <article class="contenedor-proyectos">
      <% for (let i = 0; i < projects.length; i++) { %>
        <ul class="caja-texto-producto">
        
          <% if (projects[i].photo) { %>
            <a href="/database/<%=projects[i].id%>">
            <img src="./images/<%= projects[i].photo %>" width="150"
            height="150">
          </a>
            <% } %>
            <div class="preciosYdescuentos">
          Proyecto: <%= projects[i].name %>
          
            <% for (let j = 0; j < projects[i].users.length; j++) { %>
              <li>Usuario: <%= projects[i].users[j].name %></li>
            <% } %>
          
        
          </ul>
          <% } %>
  </article>
      
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous"></script>
    </body>
</html>